import React, { useState } from "react";
import { Text, Flex, Box } from "rebass";

const IPs = () => {
  return (
      <Box>
        <Text color="white">IP</Text>
      </Box>
  );
};

export default IPs;
