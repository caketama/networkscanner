import React, { useState } from "react";
import { Text, Flex, Box } from "rebass";

const IPs = () => {
  return (
    <Flex>
      <Box mx="auto">
        <Text color="white">IP</Text>
        </Box>
    </Flex>
  );
};

export default IPs;
