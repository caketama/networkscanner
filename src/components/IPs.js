import React, { useState } from "react";
import { Text, Flex } from "rebass";

const IPs = () => {
  return (
    <Text color="#36f300" fontSize={6}>
      IP
    </Text>
  );
};

export default IPs;
