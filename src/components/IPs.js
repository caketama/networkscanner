import React, { useState } from "react";
import { Text, Flex, Box } from "rebass";

const IPs = () => {
  return (
    <Box mx="auto">
        <Text sx={{
          color: "white",
          fontSize: 5 
        }}>IP</Text>
      </Box>
  );
};

export default IPs;
