import React, { useState } from "react";
import { Text, Flex, Box } from "rebass";

const IPs = () => {
  return (
        <Text sx={{
          color: "white",
          fontSize: 5 
        }}>IP</Text>
  );
};

export default IPs;
